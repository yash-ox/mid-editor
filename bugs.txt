-- { PLAYGROUND }

set up better loading screen ( loader.tsx ) - for now changed colour to grey-600

// Only a single WebContainer instance can be booted ( fix this next error ) -- not sure if its even there or not 
          
         

-- { DASHBOARD }

add svg / logos
implement ' see all playgound ' func
open github repo 

copy url icon fix

add draggable false in template selection
add better add-new.svg ( select template svg )

change red colour to match with homepage
change add-new.svg


-- { MAIN PAGE } 



-- { SIGN-IN PAGE }

add more ss 


-- {  TERMINAL  }




-- { fixes }

homepage user mail width fix
fixed web logo
removed user redirected to playgound after creating temp func 
fixed project not registering on sidebar after project creation

termianl resize done
added go back btn in playground

fav button color fixed
Cannot read properties of undefined (reading 'dimensions') - fixed

minor text selection / img draggable fix

after closing preview and re-opening, the loading screen is stuck at Starting development server - fixed
( Reconnecting to the existing WebContainer session...) - after reloading the window its working but it restarts the while server and starts to reinstall dependencies - fixed

current session storage in web local storage ( if possible ) ( loading bar irritates ) - no need !!

add file / folder hierarchy - done
add main background mesh design or smthg boxy 

added ui changes to sign-in page

watch chatGpt chat and add theme related svg manipulation - done

after installing dependencies the downloaded dependencies should register in package.json - implemented

after opening any file then zooming in / out in ( file explorer ) the ( save / save all ) ui glitches - for now no issue is there related to this
fixed save all shortcut key not working 

basic inline suggestions - no need

add ThemeToggle in playgound
fix ThemeToggle and Go Back button ui

add new homepage hero.svg 
fix Get Started button ui